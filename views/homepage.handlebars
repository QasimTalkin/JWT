<div class="container">
  <div class="row">
    {{#each projectData}}
    <div class="col-md-4">
      <div class="thumbnail">
        <img src="https://picsum.photos/200">
        <div class="caption">
          <h3>{{project_name}}</h3>
          <p>{{project_description}}</p>
          <p><a href="{{{url}}}" class="btn btn-primary" role="button">{{project_code}}</a></p>
        </div>
      </div>
    </div>
  {{/each}}
  </div>
</div>